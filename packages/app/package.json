{
  "name": "@lab/app",
  "version": "0.0.0",
  "private": true,
  "description": "project to reproduce issues with ts-jest and mts files",
  "type": "module",
  "scripts": {
    "test:typecheck": "tsc --project tsconfig.jest.json --noemit",
    "test:ts-jest": "jest --config jest.ts-jest.config.mjs --no-cache",
    "test:swc": "jest --config jest.swc.config.mjs --no-cache",
    "build": "tsc"
  },
  "dependencies": {
    "@lab/commons": "*"
  },
  "author": "",
  "license": "ISC"
}
