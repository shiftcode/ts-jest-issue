{
  "name": "ts-jest-issue",
  "scripts": {
    "clean": "npm run clean --workspaces",
    "test:ts-jest": "npm run clean && npm run test:ts-jest --workspaces",
    "test:tsc-and-jest": "npm run clean && npm run build --workspace=@lab/commons && npm run build:test --workspace=@lab/app && npm run test:plain --workspace=@lab/app",
    "test:swc": "npm run clean && npm run build --workspaces && npm run test:swc --workspaces"
  },
  "workspaces": [
    "packages/commons",
    "packages/app"
  ],
    "devDependencies": {
      "@types/jest": "^29.2.0",
      "jest": "^29.2.1",
      "ts-jest": "^29.0.3",
      "@swc/jest": "^0.2.23",
      "@swc/core": "^1.3.10",
      "@swc/cli": "^0.1.57",
      "typescript": "^4.7.4"
    },
    "engines": {
      "node": ">=16.13.0",
      "npm": ">=8.1.2"
  }
}